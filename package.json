{
  "name": "scholar-stream-yield",
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier-plugin-solidity": "^1.3.1"
  }
}
